# Lucid MDM — demo

A static, self-contained demo of a deterministic, provenance-first Master Data Management engine.
All data is fictional. `index.html` is the landing page; `demo.html` is the end-to-end run.
